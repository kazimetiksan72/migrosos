{
  "name": "migrosos",
  "version": "1.0.0",
  "description": "MigrosOS - Sipariş Teklif Formu",
  "scripts": {
    "setup": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently --kill-others-on-fail --prefix-colors \"bgRed.bold,bgBlue.bold\" --names \"BACKEND,FRONTEND\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "npm run setup && npm run dev",
    "docker:build": "docker compose build",
    "docker:up": "docker compose up",
    "docker:prod": "docker compose up --build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
